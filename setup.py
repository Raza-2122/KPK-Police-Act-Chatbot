from setuptools import setup, find_packages

setup(
    name='kpk-police-act-chatbot',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your dependencies here
        'streamlit',
        'other_dependencies',
    ],
)
